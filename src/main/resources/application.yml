server:
  port: 8080

max:
  api:
    url: https://api.max.ru
    token: ${max.api.token}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
